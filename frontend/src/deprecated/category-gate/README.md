# Deprecated Category Gate

This folder keeps the old start-screen category selector for reference.
The active app now starts directly on the swipe deck and exposes categories through the filters panel.
